import { styled, Grid } from '@mui/material';

const Formulario = styled(Grid)`
    display:flex;
    margin: 0 auto
    width: 100%;
    max-width: 100%;
    height: 100%;
`;

export { Formulario };
